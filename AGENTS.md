# AGENTS.md

This repository's agent/AI guidance lives in **[CLAUDE.md](./CLAUDE.md)**.

Read `CLAUDE.md` first — it documents the module layout (`backend/` API server, `api/` batch collector), build/test/run commands, the manual database-schema workflow, and project conventions. Follow it as the single source of truth for working in this repo.
